{% extends "_layout" %}

{% block content %}

	<h1>{{ entry.title }} <i class="fab fa-angellist"></i></h1>

	<div class="section section--light">
		<div class="container">
			<div class="grid">
				<img src="{{ entry.image.first().getUrl() }}">
			</div>
		</div>
	</div>
	
{% endblock %}
