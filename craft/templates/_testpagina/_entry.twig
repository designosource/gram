{% extends "_layout" %}

{% block content %}

    <div class="section section--default">
        <div class="container">
            <h1>{{ entry.title }}</h1>
            <div class="test-intro">{{ entry.intro }}</div>
        </div>
    </div>

{% endblock %}